# Plan 3.7: katakana-ruby + fix del alineador — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax.
> Spec: `docs/superpowers/specs/2026-07-09-plan-3.7-katakana-design.md`. PC secundaria (Java/Android OK).

**Goal:** Lectura en hiragana sobre runs de katakana en el lector (toggle カナ independiente, default ON) y datos de furigana sin spans solapados (fix del alineador + catálogo regenerado).

**Architecture:** Frente B primero (`historias/`: fix de `limpiar_linea` en aozora.py — el walk hacia atrás de rubies sin `｜` no frena en el ruby anterior; check de spans disjuntos en verify_catalogo; regeneración). Frente A después (`app/`: partición de sub-segmentos sin furigana por runs de katakana con `lecturaKana` precomputada en el VM; toggle カナ persistido). Cierre: validación en dispositivo + ESTADO + PR.

**Tech Stack:** El de siempre (Python/unittest para historias; Kotlin/Compose/JUnit4 para app; patrones post-3.6).

## Global Constraints

- Branch `feature/plan-3.7-katakana` desde main actualizado; PR al final. Prefijos `fix(historias):`, `feat(app):`, `docs(...)`.
- Código/comentarios español; strings de UI en inglés (el toggle muestra "カナ ON/OFF" — texto japonés OK).
- **Contrato furigana**: `[inicio, fin, lectura]` fin EXCLUSIVO; tras el fix, spans ordenados y DISJUNTOS por oración (check nuevo de verify_catalogo; el catálogo actual DEBE fallar el check antes del fix — 13 solapamientos en 3 de 4 cuentos — y pasar después de regenerar).
- **Regla de runs de katakana (spec)**: run = empieza con carácter convertible (ァ..ヶ) y puede contener `ー` después (カード → かーど); `ー` sin katakana previo NO inicia run. Segmentos con furigana de kanji: intactos (furigana manda). `lecturaKana` y `lectura` nunca coexisten en un segmento.
- Pref `katakana` en tabla prefs existente, default ON (`!= "off"`), independiente del toggle de furigana.
- Patrones app establecidos: precómputo en VM (ioDispatcher, cero trabajo por frame), recomposición aislada, AGP 9.2 built-in Kotlin, tests maxHeapSize 2g. Suite base: app 78, historias 65.
- Invariante del lector intacto: concatenación de textos de segmentos == texto de la oración (test existente).
- El clamp defensivo de la app ante spans solapados (bf50f05) NO se toca.
- Orden estricto: Tasks 1-2 (historias) → 3-4 (app) → 5 (cierre). Task 2 necesita red (Aozora); Task 1 es offline.

---
## Draft — Frente B (historias/ — fix alineador + regenerar catálogo)

> Fuente: `docs/superpowers/specs/2026-07-09-plan-3.7-katakana-design.md`,
> sección "Frente B". Se ensambla junto con el draft de Frente A en
> `docs/superpowers/plans/2026-07-09-plan-3.7-katakana-design.md`. Los números
> de Task se renumeran al ensamblar.

## Root cause (investigado y reproducido localmente, sin necesitar `fuentes/`)

El bug vive en `historias/src/aozora.py:limpiar_linea` — es puramente de
parsing de texto (no depende de descargar Aozora), así que se reprodujo
directo con:

```python
from src import aozora
aozora.limpiar_linea('「おばあさん、今《いま》帰《かえ》ったよ。」')
## -> texto='「おばあさん、今帰ったよ。」'
##    furigana=[[7, 8, 'いま'], [7, 9, 'かえ']]   (bug: debería ser [8, 9])
```

Cuando un ruby `《》` no tiene marca explícita `｜`, `limpiar_linea` infiere el
inicio de su base "expandiendo hacia atrás" mientras el carácter previo sea
`japones.es_base_ruby` (kanji u ocho extras 々〆ヵヶ):

```python
inicio = len(texto)
while inicio > 0 and japones.es_base_ruby(texto[inicio - 1]):
    inicio -= 1
```

Esa expansión **no se detiene en el límite del ruby anterior**. Con
`今《いま》帰《かえ》` (dos kanjis sueltos, cada uno con su propio ruby, sin
`｜` entre ellos): al procesar `いま`, expande correctamente hasta el `今`
(inicio=7, fin=8). Al procesar `かえ`, `帰` es la base obvia (inicio debería
frenar en 8), pero el while seguiría retrocediendo porque `texto[7]` (`今`)
también es `es_base_ruby(...) == True` — son dos kanjis contiguos, el bucle no
sabe que el primero **ya fue consumido** por el ruby anterior. Resultado:
`かえ` roba el `今` y emite `[7, 9, 'かえ']`, solapando con `[7, 8, 'いま']`.

Confirmado que el catálogo real actual (`catalogo/historias/*.json`) ya
contiene el bug en 3 de las 4 historias (no solo momotaro):

| historia | pares de spans solapados |
|---|---|
| momotaro | 3 (incluye el caso `今`/`帰` del spec) |
| urashima_taro | 0 |
| issunboshi | 8 |
| kachikachi_yama | 2 |

Fix (una condición): frenar la expansión hacia atrás en el `fin` del último
ruby ya emitido, no en `0`:

```python
inicio = len(texto)
limite = furigana[-1][1] if furigana else 0
while inicio > limite and japones.es_base_ruby(texto[inicio - 1]):
    inicio -= 1
```

Verificado a mano contra los 6 tests existentes de `test_aozora.py` (todos
usan un solo ruby o rubies separados por un carácter no-kanji, así que
`limite` nunca dispara — cero regresión) y contra el caso real (con el fix,
`かえ` da `[8, 9]`, ya no solapa con `[7, 8]`).

`historias/src/segmentador.py:segmentar_parrafo` solo reindexa spans ya
producidos por `aozora.parsear` (resta `inicio` del párrafo) — no reordena ni
recorta, así que no puede introducir ni ocultar el solapamiento: el bug es
100% atribuible a `aozora.limpiar_linea`.

### Task 1: fix del alineador (TDD) + check nuevo en verify_catalogo

**Files:**
- Modify: `historias/src/aozora.py`
- Modify: `historias/verify_catalogo.py`
- Test: `historias/tests/test_aozora.py`
- Test: `historias/tests/test_verify.py`

**Interfaces:**
- Consumes: `japones.es_base_ruby(caracter: str) -> bool` (sin cambios).
- Produce: `aozora.limpiar_linea(linea: str) -> tuple[str, list]` — mismo
  contrato `[inicio, fin, lectura]` fin exclusivo, ahora sin solapamientos
  entre rubies consecutivos de la misma línea.
- Produce: `verify_catalogo.verificar(dir_catalogo, imprimir=False) -> list`
  — mismo contrato (lista de strings de error), ahora también detecta spans
  de `furigana` desordenados o solapados dentro de una oración.

- [ ] **Step 1: Write the failing test**

  En `historias/tests/test_aozora.py`, agregar a `TestLimpiarLinea` (después
  de `test_multiples_rubies_en_una_linea`):

  ```python
      def test_rubies_adyacentes_no_solapan(self):
          # caso real momotaro: dos kanjis sueltos consecutivos, cada uno
          # con su propio ruby y sin ｜ entre ellos — el segundo ruby no
          # debe "robarle" el carácter base al primero.
          texto, furigana = aozora.limpiar_linea(
              '「おばあさん、今《いま》帰《かえ》ったよ。」')
          self.assertEqual(texto, '「おばあさん、今帰ったよ。」')
          self.assertEqual(furigana, [[7, 8, 'いま'], [8, 9, 'かえ']])
  ```

  En `historias/tests/test_verify.py`, agregar a `TestVerify` (después de
  `test_furigana_tipos_invalidos_se_reporta_sin_excepcion`):

  ```python
      def test_detecta_furigana_solapada_o_desordenada(self):
          def mutar(h):
              # しば刈りに。 (6 caracteres) — ambos spans son individualmente
              # válidos (0<=inicio<fin<=6) pero se solapan entre sí.
              h['parrafos'][0]['oraciones'][1]['furigana'] = (
                  [[0, 3, 'a'], [2, 5, 'b']])
          self._corromper_historia(mutar)
          errores = verify_catalogo.verificar(self.tmp)
          self.assertTrue(any('solapad' in e for e in errores))
  ```

- [ ] **Step 2: Run test to verify it fails**

  ```bash
  cd historias && python3 -m unittest tests.test_aozora.TestLimpiarLinea.test_rubies_adyacentes_no_solapan tests.test_verify.TestVerify.test_detecta_furigana_solapada_o_desordenada -v
  ```

  Falla esperada (reproducida durante la investigación):

  ```
  FAIL: test_rubies_adyacentes_no_solapan
  AssertionError: Lists differ: [[7, 8, 'いま'], [7, 9, 'かえ']] != [[7, 8, 'いま'], [8, 9, 'かえ']]

  FAIL: test_detecta_furigana_solapada_o_desordenada
  AssertionError: False is not true
  ```

  (el segundo falla porque el check nuevo todavía no existe en
  `verify_catalogo.py` — `errores` queda vacío).

  Además, correr el catálogo real contra el check nuevo una vez escrito
  (Step 3) para confirmar que HOY falla (evidencia de que el check detecta el
  bug de producción, no solo el fixture sintético):

  ```bash
  cd historias && python3 verify_catalogo.py ../catalogo
  ```

  Debe reportar errores de solapamiento en momotaro/issunboshi/kachikachi_yama
  (ver tabla de arriba) — si no falla, el check nuevo está mal escrito.

- [ ] **Step 3: Write minimal implementation**

  En `historias/src/aozora.py`, dentro de `limpiar_linea`, reemplazar el
  bloque `else` de la rama `《`:

  ```python
              if inicio_marcado is not None:
                  inicio = inicio_marcado
              else:
                  inicio = len(texto)
                  limite = furigana[-1][1] if furigana else 0
                  while inicio > limite and japones.es_base_ruby(texto[inicio - 1]):
                      inicio -= 1
  ```

  (único cambio: se agrega la línea `limite = ...` y la condición del `while`
  pasa de `inicio > 0` a `inicio > limite`).

  En `historias/verify_catalogo.py`, dentro de `_verificar_historia`, en el
  loop `for o, oracion in enumerate(...)`, agregar el rastreo de solapamiento
  junto al loop existente de `furigana` (reemplazar el bloque completo del
  `for furi in oracion.get('furigana', []):`):

  ```python
              anterior_fin = None
              for furi in oracion.get('furigana', []):
                  if not isinstance(furi, list) or len(furi) != 3:
                      errores.append(f'{donde}: furigana inválida {furi}')
                      continue
                  inicio, fin, lectura = furi
                  if (not isinstance(inicio, int) or not isinstance(fin, int)
                          or not isinstance(lectura, str)):
                      errores.append(f'{donde}: furigana inválida {furi}')
                      continue
                  if not (0 <= inicio < fin <= len(texto)) or not lectura:
                      errores.append(f'{donde}: furigana inválida {furi}')
                      continue
                  if anterior_fin is not None and inicio < anterior_fin:
                      errores.append(
                          f'{donde}: furigana solapada o desordenada {furi} '
                          f'(fin anterior {anterior_fin})')
                  anterior_fin = fin
  ```

  (nota: el `continue` de las ramas de "furigana inválida" hace que
  `anterior_fin` no se actualice con spans malformados — el check de
  solapamiento solo compara spans bien formados entre sí, que es lo correcto:
  un span malformado ya se reporta por su cuenta).

- [ ] **Step 4: Run test to verify it passes**

  ```bash
  cd historias && python3 -m unittest discover tests -v
  ```

  Esperado: 67 tests OK (65 base + 2 nuevos), sin regresiones en
  `test_aozora.py` (los 6 tests existentes de rubies siguen pasando: ninguno
  tiene dos rubies con kanjis contiguos sin separador, así que `limite` nunca
  se activa para ellos) ni en `test_verify.py`.

- [ ] **Step 5: Commit**

  ```bash
  git add historias/src/aozora.py historias/verify_catalogo.py historias/tests/test_aozora.py historias/tests/test_verify.py
  git commit -m "fix(historias): alineador no solapa rubies de kanjis contiguos + check de disjunción en verify_catalogo"
  ```

### Task 2: regenerar catalogo/ con el pipeline real + sincronizar fixtures de la app

**Files:**
- Create (gitignored, no se commitea): `historias/fuentes/*.txt` (4 archivos)
- Modify: `catalogo/catalogo.json`, `catalogo/historias/*.json` (4 archivos)
- Modify: `app/app/src/test/resources/catalogo.json`,
  `app/app/src/test/resources/momotaro.json`
- Test: ninguno nuevo (este task es de datos; los gates son los tests
  existentes de Task 1 + la suite de la app)

**Interfaces:**
- Consumes: `historias/pipeline.py` CLI (`python3 pipeline.py`, lee
  `fuentes/*.txt` + `obras.json`, escribe en `../catalogo`).
- Consumes: `historias/verify_catalogo.py` CLI (exit 1 si falla).
- Produce: `catalogo/catalogo.json` v2 y `catalogo/historias/{momotaro,
  urashima_taro, issunboshi, kachikachi_yama}.json` — mismo schema, sin
  cambios de `kanjis_unicos`/`oraciones` (derivan de kanjis y conteo de
  oraciones, no de los spans de furigana), solo con los spans de `furigana`
  corregidos donde había solapamiento.

- [ ] **Step 1: Write the failing test**

  No aplica test unitario nuevo (Task 1 ya cubre el código). El "test" de
  este paso es la evidencia de que el catálogo actual (sin regenerar)
  **falla** el check nuevo — ya se demostró en la investigación y se repite
  aquí como gate de entrada:

  ```bash
  cd historias && python3 verify_catalogo.py ../catalogo
  ```

  Debe reportar (con el fix de Task 1 ya aplicado, pero el catálogo viejo
  todavía sin regenerar):

  ```
  ✗ momotaro p... o...: furigana solapada o desordenada [7, 9, 'かえ'] (fin anterior 8)
  ✗ issunboshi ...: furigana solapada o desordenada ... (8 ocurrencias)
  ✗ kachikachi_yama ...: furigana solapada o desordenada ... (2 ocurrencias)
  ```

  y `sys.exit(1)`.

- [ ] **Step 2: Run test to verify it fails**

  (mismo comando del Step 1 — confirmar el exit code)

  ```bash
  cd historias && python3 verify_catalogo.py ../catalogo; echo "exit=$?"
  ```

  Esperado: `exit=1` con los errores de solapamiento listados arriba.

- [ ] **Step 3: Write minimal implementation**

  3a. Descargar las 4 fuentes de Aozora a `historias/fuentes/` (gitignored;
  ver `historias/README.md` sección "Descarga de fuentes"):

  ```bash
  cd historias && mkdir -p fuentes && cd fuentes
  wget https://www.aozora.gr.jp/cards/000329/files/18376_ruby_12074.zip
  unzip -o 18376_ruby_12074.zip && rm 18376_ruby_12074.zip && mv *.txt momotaro.txt
  wget https://www.aozora.gr.jp/cards/000329/files/3390_ruby_6090.zip
  unzip -o 3390_ruby_6090.zip && rm 3390_ruby_6090.zip && mv *.txt urashima_taro.txt
  wget https://www.aozora.gr.jp/cards/000329/files/43457_ruby_23696.zip
  unzip -o 43457_ruby_23696.zip && rm 43457_ruby_23696.zip && mv *.txt issunboshi.txt
  wget https://www.aozora.gr.jp/cards/000329/files/18377_ruby_11923.zip
  unzip -o 18377_ruby_11923.zip && rm 18377_ruby_11923.zip && mv *.txt kachikachi_yama.txt
  cd ..
  ```

  3b. Regenerar el catálogo:

  ```bash
  cd historias && python3 pipeline.py
  ```

  3c. Gate 1 — verify_catalogo (con el check nuevo de Task 1):

  ```bash
  cd historias && python3 verify_catalogo.py ../catalogo
  ```

  Debe imprimir `✓ OK` (exit 0) — si vuelve a fallar por solapamiento, el fix
  de Task 1 no llegó a cubrir todos los casos reales; investigar antes de
  seguir.

  3d. Gate 2 — sanity explícita de no-solapamiento sobre los 4 JSON (doble
  chequeo independiente del check integrado en verify_catalogo, corrido a
  mano contra los archivos regenerados):

  ```bash
  cd historias && python3 -c "
  import json
  for f in ('momotaro', 'urashima_taro', 'issunboshi', 'kachikachi_yama'):
      d = json.load(open(f'../catalogo/historias/{f}.json', encoding='utf-8'))
      n = 0
      for p in d['parrafos']:
          for o in p['oraciones']:
              furi = o.get('furigana', [])
              for i in range(len(furi) - 1):
                  if furi[i][1] > furi[i + 1][0]:
                      n += 1
              # ademas: nunca disminuye el inicio (orden)
              inicios = [s[0] for s in furi]
              assert inicios == sorted(inicios), f'{f}: spans desordenados'
      print(f'{f}: solapamientos={n}')
      assert n == 0, f'{f} tiene solapamientos'
  print('sanity OK: 0 solapamientos en las 4 historias')
  "
  ```

  3e. Gate 3 — diff esperado: confirmar que el diff son spans de `furigana`
  corregidos y NADA MÁS (ni `kanjis_unicos` ni `oraciones` cambian, porque
  ambos derivan de conteo de kanjis/oraciones, no de posiciones de furigana):

  ```bash
  git diff --stat catalogo/
  git diff catalogo/catalogo.json   # kanjis_unicos y oraciones por historia: sin cambios
  git diff catalogo/historias/momotaro.json | grep -E '^\+.*furigana|^-.*furigana' | head -20
  ```

  Si `git diff catalogo/catalogo.json` muestra cambios en `kanjis_unicos` o
  `oraciones` de alguna historia, DETENERSE y documentar la causa exacta
  antes de seguir (no se espera ningún cambio ahí — indicaría que el fix de
  Task 1 alteró qué se considera texto/kanji, lo cual sería una regresión
  fuera de alcance).

  3f. Commitear el catálogo regenerado:

  ```bash
  git add catalogo/
  git commit -m "fix(historias): regenerar catalogo/ con el alineador corregido (spans sin solapar)"
  ```

  3g. Sincronizar las fixtures reales de la app (copias byte-idénticas):

  ```bash
  cp catalogo/catalogo.json app/app/src/test/resources/catalogo.json
  cp catalogo/historias/momotaro.json app/app/src/test/resources/momotaro.json
  diff catalogo/catalogo.json app/app/src/test/resources/catalogo.json && echo "catalogo.json: idéntico"
  diff catalogo/historias/momotaro.json app/app/src/test/resources/momotaro.json && echo "momotaro.json: idéntico"
  ```

- [ ] **Step 4: Run test to verify it passes**

  Gate final — suite completa de `historias/` (base 65 + 2 de Task 1 = 67):

  ```bash
  cd historias && python3 -m unittest discover tests -v
  ```

  Gate final — suite completa de la app, para confirmar que los asserts que
  dependen de `kanjis_unicos`/`oraciones` (p.ej.
  `LectorViewModelTest.kt`/`ParserHistoriaTest.kt` línea 34 y
  `BibliotecaViewModelTest.kt` línea 85, ambos asumen `174` oraciones en
  momotaro) siguen verdes con las fixtures regeneradas:

  ```bash
  cd app && ./gradlew test
  ```

  Si `oraciones` de momotaro cambia de 174 (no debería, ver Step 3e), estos
  dos asserts van a fallar por número — en ese caso, actualizar el literal
  `174` en ambos archivos junto con una nota explicando por qué cambió
  (esperado: NO va a pasar, ya que el fix del alineador solo mueve límites de
  `furigana`, no agrega/quita kanjis ni oraciones).

- [ ] **Step 5: Commit**

  ```bash
  git add app/app/src/test/resources/catalogo.json app/app/src/test/resources/momotaro.json
  git commit -m "fix(historias): sincronizar fixtures de la app con el catalogo regenerado (spans sin solapar)"
  ```

  (fuentes/ nunca se commitea — está gitignored; si `git status` muestra
  `historias/fuentes/` como untracked, es esperado y no se agrega).

## Binding constraints verificadas

- Código/comentarios en español: todo el código nuevo arriba está en
  español, consistente con el resto de `historias/src/`.
- Layout de tests existente: los 2 tests nuevos van dentro de las clases
  `TestLimpiarLinea` (`test_aozora.py`) y `TestVerify` (`test_verify.py`) ya
  existentes, mismo estilo (`unittest`, nombres `test_<qué_verifica>`).
- Contrato `[inicio, fin, lectura]` fin exclusivo: intacto — el fix de Task 1
  no cambia el contrato, solo corrige qué `inicio` se calcula.
- "El catálogo actual debe fallar el check nuevo antes del fix": verificado
  en la investigación (`momotaro`: 3 pares solapados, `issunboshi`: 8,
  `kachikachi_yama`: 2) y repetido como gate explícito en Task 2 Step 1-2.
- Sin placeholders: todo el código de los steps está completo y fue
  verificado a mano (trazas manuales) contra el caso real y contra los 6
  tests preexistentes de rubies en `test_aozora.py`.

## Riesgos / notas para quien ejecute

- Task 2 requiere red (descarga de 4 zips de Aozora) — si el entorno de
  ejecución no tiene acceso a `aozora.gr.jp`, Task 2 queda bloqueado; Task 1
  es 100% offline (no toca `fuentes/`).
- El fix de Task 1 es una condición de un carácter (`inicio > 0` →
  `inicio > limite`) pero su corrección depende de que `furigana` esté
  ordenada por `fin` ascendente al momento de cada nueva expansión — eso ya
  es cierto hoy porque `limpiar_linea` procesa la línea de izquierda a
  derecha y appendea en orden, pero si en el futuro se reordena esa función,
  revisar esta invariante.
- No se investigó si `issunboshi`/`kachikachi_yama` tienen además OTRO patrón
  de solapamiento distinto al de "dos kanjis contiguos sin ｜" (p.ej. tres o
  más kanjis contiguos con rubies individuales) — el fix propuesto generaliza
  correctamente a N rubies contiguos porque cada `limite` se recalcula contra
  el último span emitido, pero esto se confirma recién en Task 2 Step 3c
  (verify_catalogo debe dar `✓ OK` sobre las 4 historias reales, no solo
  sobre el caso de momotaro).
## Plan 3.7 — Frente A (`app/`, katakana-ruby) — draft de tareas

> Draft parcial: cubre únicamente el Frente A (Android) del spec
> `docs/superpowers/specs/2026-07-09-plan-3.7-katakana-design.md`. Se ensambla junto
> con el draft del Frente B (`historias/`, fix del alineador + regeneración de
> `catalogo/`) en el plan final. Tareas numeradas 1..2 acá; se renumeran al ensamblar.
>
> **Para agentic workers:** REQUIRED SUB-SKILL: usar `superpowers:subagent-driven-development`
> (recomendado) o `superpowers:executing-plans` para ejecutar tarea por tarea.

**Goal:** Ruby de katakana→hiragana en el lector (onomatopeyas y palabras sueltas en
katakana, ej. ドンブラコッコ), con el mismo mecanismo de sub-segmentos que la furigana
de kanji, toggle propio "カナ ON/OFF" independiente del de Furigana, y precómputo en
el ViewModel (cero trabajo por frame, patrón ya establecido en Plan 3.6).

**Architecture:** Extiende, byte-a-byte, las estructuras reales post Plan 3.6:
`Segmento` (dato), `calcularGruposFurigana`/`agruparTokens`/`segmentosDeGrupo`
(precómputo puro en `ui/lector/TextoConFurigana.kt`), `LectorViewModel.cargar()`
(precómputo en `ioDispatcher`), `PrefsRepo` (tabla `prefs` existente, patrón
`clave != "off"`). La conversión katakana→hiragana es la misma función determinística
carácter-a-carácter que ya usa `Tokenizador` para la lectura de Kuromoji — se saca de
la clase (privada, exige instanciar el `Tokenizer` caro) a top-level `internal` en el
mismo archivo, reusable sin instanciar nada.

**Tech Stack:** Kotlin, Jetpack Compose M3, Room 2.8.4, Kuromoji IPADIC,
kotlinx.serialization, JUnit4 + Robolectric, AGP 9.2 (Kotlin built-in, sin
`kotlinOptions`).

## Global Constraints (del spec, aplican a toda tarea)

- Código/comentarios en español. Strings de UI en inglés — **excepción**: el toggle
  muestra literalmente "カナ ON"/"カナ OFF" (カナ es texto japonés, no traducible, mismo
  criterio que ya usa la UI para nombres/lecturas japonesas).
- Patrón `ioDispatcher: CoroutineDispatcher = Dispatchers.IO` inyectable, precómputo
  SIEMPRE en `LectorViewModel.cargar()`, nunca en composición (costo por frame) ni en
  el pipeline de datos (acoplaría una preferencia de UI al JSON).
- `PrefsRepo`: nueva clave `"katakana"` sobre la tabla `prefs` existente, mismo
  patrón que `"furigana"` (`!= "off"` ⇒ activa, default `true`).
- Invariante existente intacto: concatenación de textos de segmentos == texto de la
  oración (test `invariante- ninguna oracion de momotaro...` sigue verde sin tocarlo,
  porque la partición de katakana es un paso posterior que no cambia esa función).
- Un `Segmento` nunca tiene `lectura` y `lecturaKana` simultáneamente (la furigana de
  kanji manda; la partición de katakana solo actúa sobre sub-segmentos ya sin
  furigana).
- Antes de cada commit: `cd app && ./gradlew test` verde + `./gradlew assembleDebug`
  sin errores. Suite base 78 + los tests nuevos de este frente.
- No placeholders: cada step lleva código completo, comandos exactos.

---

### Task 3: Partición de katakana en sub-segmentos + persistencia del toggle

**Files:**
- Modify: `app/app/src/main/kotlin/com/tatoh/dokushorenshu/dominio/Tokenizador.kt`
- Modify: `app/app/src/main/kotlin/com/tatoh/dokushorenshu/ui/lector/TextoConFurigana.kt`
- Modify: `app/app/src/main/kotlin/com/tatoh/dokushorenshu/datos/progreso/PrefsRepo.kt`
- Test: `app/app/src/test/kotlin/com/tatoh/dokushorenshu/ui/lector/LectorViewModelTest.kt`
- Test: `app/app/src/test/kotlin/com/tatoh/dokushorenshu/datos/progreso/ProgresoDaoTest.kt`

**Interfaces:**
- Consumes: `PalabraToken(superficie, lecturaHiragana, formaBase, inicio, fin, esContenido)`
  (`dominio/Tokenizador.kt`, sin cambios); `Furigana(inicio, fin, lectura)`
  (`datos/ModelosHistoria.kt`, sin cambios); `Segmento(texto, lectura, token)`
  existente; `ProgresoDao.pref(clave)`/`guardarPref(Pref)` (patrón de `PrefsRepo`).
- Produces:
  - `internal fun katakanaAHiragana(katakana: String): String` — top-level en
    `dominio/Tokenizador.kt` (antes `private fun` de la clase `Tokenizador`; mismo
    cuerpo, ahora reusable sin instanciar el `Tokenizer` de Kuromoji).
  - `data class Segmento(val texto: String, val lectura: String?, val token: PalabraToken, val lecturaKana: String? = null)`
    — campo nuevo, default `null` (no rompe ninguna construcción/assert existente).
  - `internal fun particionarPorKatakana(segmentos: List<Segmento>): List<Segmento>`
    — nueva función pura en `ui/lector/TextoConFurigana.kt`.
  - `calcularGruposFurigana(tokens, furigana): List<GrupoFurigana>` — MISMA firma,
    ahora aplica `particionarPorKatakana` a los segmentos de cada grupo antes de
    envolverlos en `GrupoFurigana` (precómputo siempre corre, el toggle de UI del
    Task 4 solo decide si se DIBUJA `lecturaKana`, nunca si se calcula).
  - `PrefsRepo.katakanaActiva(): Boolean` / `PrefsRepo.setKatakanaActiva(activa: Boolean)`
    — mismo patrón que `furiganaActiva()`/`setFuriganaActiva()`, clave `"katakana"`.

- [ ] **Step 1: Write the failing test**

Agregar a `LectorViewModelTest.kt`, después del bloque `--- calcularGruposFurigana ---`
(antes del test `invariante- ninguna oracion de momotaro...`):

```kotlin
    // --- particionarPorKatakana: split de un sub-segmento SIN furigana en runs de
    // katakana (Plan 3.7, katakana-ruby). Un run empieza con un carácter convertible
    // (ァ..ヶ) y puede seguir con ー (se preserva tal cual, カード → かーど); un ー sin
    // katakana inmediatamente antes NO inicia run. Furigana de kanji manda: un
    // segmento con `lectura != null` queda intacto, nunca se parte. ---

    @Test
    fun `onomatopeya pura da un solo run con su hiragana`() {
        val token = PalabraToken("ドンブラコッコ", null, null, inicio = 0, fin = 7, esContenido = true)
        val segmento = Segmento("ドンブラコッコ", null, token)
        assertEquals(
            listOf(Segmento("ドンブラコッコ", null, token, "どんぶらこっこ")),
            particionarPorKatakana(listOf(segmento)),
        )
    }

    @Test
    fun `token mixto separa el run de katakana del resto sin lectura`() {
        val token = PalabraToken("アイス食べ", null, null, inicio = 0, fin = 5, esContenido = true)
        val segmento = Segmento("アイス食べ", null, token)
        assertEquals(
            listOf(
                Segmento("アイス", null, token, "あいす"),
                Segmento("食べ", null, token, null),
            ),
            particionarPorKatakana(listOf(segmento)),
        )
    }

    @Test
    fun `segmento con furigana de kanji intacta no se parte por katakana`() {
        val token = PalabraToken("カード", null, null, inicio = 0, fin = 3, esContenido = true)
        val segmento = Segmento("カード", "かあど", token) // furigana explícita (dato hipotético)
        assertEquals(listOf(segmento), particionarPorKatakana(listOf(segmento)))
    }

    @Test
    fun `run de katakana con prolongador ー se preserva tal cual en la lectura`() {
        val token = PalabraToken("カード", null, null, inicio = 0, fin = 3, esContenido = true)
        val segmento = Segmento("カード", null, token)
        assertEquals(
            listOf(Segmento("カード", null, token, "かーど")),
            particionarPorKatakana(listOf(segmento)),
        )
    }

    @Test
    fun `ー suelto sin katakana antes no inicia run y queda sin lectura`() {
        val token = PalabraToken("ーです", null, null, inicio = 0, fin = 3, esContenido = true)
        val segmento = Segmento("ーです", null, token)
        assertEquals(
            listOf(Segmento("ーです", null, token, null)),
            particionarPorKatakana(listOf(segmento)),
        )
    }

    @Test
    fun `segmento sin ningun katakana queda sin cambios`() {
        val token = PalabraToken("むかしむかし", null, null, inicio = 0, fin = 6, esContenido = true)
        val segmento = Segmento("むかしむかし", null, token)
        assertEquals(listOf(segmento), particionarPorKatakana(listOf(segmento)))
    }

    @Test
    fun `calcularGruposFurigana aplica la particion de katakana sin furigana en el grupo`() {
        val token = PalabraToken("ドンブラコッコ", null, null, inicio = 0, fin = 7, esContenido = true)
        val grupos = calcularGruposFurigana(listOf(token), emptyList())
        assertEquals(
            listOf(Segmento("ドンブラコッコ", null, token, "どんぶらこっこ")),
            grupos.single().segmentos,
        )
    }
```

Agregar a `ProgresoDaoTest.kt`, junto al test `prefs roundtrip y default de furigana`:

```kotlin
    @Test
    fun `prefs roundtrip y default de katakana`() = runTest {
        val repo = PrefsRepo(db().dao())
        assertTrue(repo.katakanaActiva())  // default: activada
        repo.setKatakanaActiva(false)
        assertFalse(repo.katakanaActiva())
    }
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd app && ./gradlew testDebugUnitTest --tests '*LectorViewModelTest*' --tests '*ProgresoDaoTest*'`
Expected: FAIL en compilación — `particionarPorKatakana` no existe todavía,
`Segmento(texto, lectura, token, lecturaKana)` no compila (constructor de 3 args),
`PrefsRepo.katakanaActiva()`/`setKatakanaActiva()` no existen.

- [ ] **Step 3: Write minimal implementation**

`dominio/Tokenizador.kt` completo:

```kotlin
package com.tatoh.dokushorenshu.dominio

import com.atilika.kuromoji.ipadic.Tokenizer

/** Span [inicio, fin) exclusivo sobre el texto de la oración. */
data class PalabraToken(
    val superficie: String,
    val lecturaHiragana: String?,
    val formaBase: String?,
    val inicio: Int,
    val fin: Int,
    val esContenido: Boolean,
)

/** Wrapper de Kuromoji IPADIC. La instancia de Tokenizer es cara (~1s carga
 *  de diccionario): crear UNA por app (vive en el Contenedor). */
class Tokenizador {
    private val tokenizer = Tokenizer()

    // categorías gramaticales que la UI no hace tappeables
    private val noContenido = setOf("助詞", "助動詞", "記号")

    fun tokenizar(texto: String): List<PalabraToken> {
        if (texto.isEmpty()) return emptyList()
        return tokenizer.tokenize(texto).map { token ->
            PalabraToken(
                superficie = token.surface,
                lecturaHiragana = token.reading
                    ?.takeIf { it != "*" }
                    ?.let(::katakanaAHiragana),
                formaBase = token.baseForm?.takeIf { it != "*" },
                inicio = token.position,
                fin = token.position + token.surface.length,
                esContenido = token.partOfSpeechLevel1 !in noContenido,
            )
        }
    }
}

/** Convierte katakana a hiragana carácter a carácter (desplazamiento fijo de 0x60 en
 *  el bloque Unicode, ァ 30A1 → ぁ 3041; caracteres fuera de ese rango, como el
 *  prolongador ー, pasan sin cambios). Top-level e `internal` (Plan 3.7: antes era
 *  `private fun` de [Tokenizador], donde solo convertía la lectura de Kuromoji; ahora
 *  la reusa también `particionarPorKatakana` en `ui/lector/TextoConFurigana.kt` para
 *  el ruby de katakana). Sacarla de la clase evita instanciar un [Tokenizador]
 *  completo —carga cara del diccionario Kuromoji, ~1s— solo para convertir texto. */
internal fun katakanaAHiragana(katakana: String): String =
    katakana.map { c ->
        if (c in 'ァ'..'ヶ') c - 0x60 else c  // ァ(30A1)→ぁ(3041)
    }.joinToString("")
```

En `ui/lector/TextoConFurigana.kt`: agregar el import y el campo nuevo a `Segmento`
(reemplazar la declaración existente por esta, con doc actualizado):

```kotlin
import com.tatoh.dokushorenshu.dominio.katakanaAHiragana
```

```kotlin
/** Un sub-tramo del `superficie` de un token (o de un cluster de tokens, ver
 *  [GrupoRenderizado]) con su propia lectura de ruby (o null si ese tramo no tiene
 *  furigana propia, p.ej. la okurigana de un verbo). [token] es el token DUEÑO del
 *  segmento a efectos de tap: para un segmento que cae dentro de un solo token es ese
 *  token; para un segmento de furigana que cruza el límite entre varios tokens del
 *  mismo grupo (ver [agruparTokens]) es el primero (más a la izquierda) de ellos.
 *
 *  [lecturaKana] (Plan 3.7, katakana-ruby): hiragana de un run de katakana dentro de
 *  este segmento, calculada por [particionarPorKatakana]. NUNCA coexiste con
 *  [lectura] — la furigana de kanji manda; la partición de katakana solo actúa sobre
 *  segmentos que YA quedaron sin furigana (`lectura == null`) tras [segmentosDeGrupo]. */
data class Segmento(val texto: String, val lectura: String?, val token: PalabraToken, val lecturaKana: String? = null)
```

Agregar, después de `segmentosDeGrupo`, la partición de katakana y su integración en
`calcularGruposFurigana` (reemplazar la función `calcularGruposFurigana` existente):

```kotlin
/** Precomputa, para TODOS los grupos de una oración, el resultado de
 *  `agruparTokens` + `segmentosDeGrupo` — las dos funciones puras que antes corrían
 *  dentro de la composición de [TextoConFurigana] en cada recompose de item (fix de
 *  performance, Plan 3.6 feedback). Se llama UNA vez por oración desde
 *  [LectorViewModel.cargar] (en `ioDispatcher`), nunca en cada composición.
 *
 *  Plan 3.7 (katakana-ruby): cada grupo pasa además por [particionarPorKatakana],
 *  que parte los sub-segmentos SIN furigana en runs de katakana con su hiragana
 *  alineada ([Segmento.lecturaKana]). Corre siempre acá, independiente de cualquier
 *  toggle de UI (mismo criterio que la furigana: precómputo único, el toggle de
 *  `TextoConFurigana` solo decide si se DIBUJA, nunca si se calcula). */
internal fun calcularGruposFurigana(tokens: List<PalabraToken>, furigana: List<Furigana>): List<GrupoFurigana> =
    agruparTokens(tokens, furigana).map { grupo ->
        GrupoFurigana(grupo, particionarPorKatakana(segmentosDeGrupo(grupo, furigana)))
    }

// Rango Unicode de katakana convertible a hiragana (mismo rango que [katakanaAHiragana]).
private val RANGO_KATAKANA = 'ァ'..'ヶ'
private const val PROLONGADOR = 'ー'

/** Parte cada [Segmento] SIN furigana (`lectura == null`) en sub-tramos de katakana
 *  (con [Segmento.lecturaKana] calculada) y de no-katakana (sin ruby) — mecanismo de
 *  katakana-ruby del Plan 3.7. Un segmento con `lectura != null` queda SIEMPRE
 *  intacto: la furigana de kanji manda, nunca se re-particiona por katakana.
 *
 *  Un run de katakana EMPIEZA con un carácter convertible (ァ..ヶ) y puede seguir con
 *  el prolongador ー en el medio o al final, preservado tal cual (カード → かーど). Un
 *  ー sin katakana inmediatamente antes NO inicia run por sí solo — queda en el tramo
 *  de no-katakana, sin ruby (ver test `ー suelto sin katakana antes no inicia run`).
 *
 *  Conversión determinística carácter a carácter vía [katakanaAHiragana] — no
 *  requiere diccionario ni lecturas de Kuromoji, así que nunca falla ni crashea. */
internal fun particionarPorKatakana(segmentos: List<Segmento>): List<Segmento> =
    segmentos.flatMap { segmento ->
        if (segmento.lectura != null) listOf(segmento) else particionarTexto(segmento.texto, segmento.token)
    }

private fun particionarTexto(texto: String, token: PalabraToken): List<Segmento> {
    if (texto.isEmpty()) return emptyList()
    val resultado = mutableListOf<Segmento>()
    var i = 0
    while (i < texto.length) {
        if (texto[i] in RANGO_KATAKANA) {
            var j = i + 1
            while (j < texto.length && (texto[j] in RANGO_KATAKANA || texto[j] == PROLONGADOR)) j++
            val run = texto.substring(i, j)
            resultado.add(Segmento(run, null, token, katakanaAHiragana(run)))
            i = j
        } else {
            var j = i + 1
            while (j < texto.length && texto[j] !in RANGO_KATAKANA) j++
            resultado.add(Segmento(texto.substring(i, j), null, token))
            i = j
        }
    }
    return resultado
}
```

`datos/progreso/PrefsRepo.kt` completo:

```kotlin
package com.tatoh.dokushorenshu.datos.progreso

/** Preferencias sobre la tabla prefs. Los toggles de furigana y katakana son
 *  globales y persisten (spec Plan 3.5 / Plan 3.7); ambos independientes entre sí. */
class PrefsRepo(private val dao: ProgresoDao) {
    suspend fun furiganaActiva(): Boolean = dao.pref(CLAVE_FURIGANA) != "off"

    suspend fun setFuriganaActiva(activa: Boolean) {
        dao.guardarPref(Pref(CLAVE_FURIGANA, if (activa) "on" else "off"))
    }

    /** Katakana-ruby (Plan 3.7): default ON, misma semántica `!= "off"` que furigana. */
    suspend fun katakanaActiva(): Boolean = dao.pref(CLAVE_KATAKANA) != "off"

    suspend fun setKatakanaActiva(activa: Boolean) {
        dao.guardarPref(Pref(CLAVE_KATAKANA, if (activa) "on" else "off"))
    }

    private companion object {
        const val CLAVE_FURIGANA = "furigana"
        const val CLAVE_KATAKANA = "katakana"
    }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd app && ./gradlew testDebugUnitTest --tests '*LectorViewModelTest*' --tests '*ProgresoDaoTest*'`
Expected: BUILD SUCCESSFUL, todos los tests (existentes + nuevos) en verde. Confirmar
también que la suite completa no se rompió: `cd app && ./gradlew test`.

- [ ] **Step 5: Commit**

```bash
git add app/app/src/main/kotlin/com/tatoh/dokushorenshu/dominio/Tokenizador.kt \
        app/app/src/main/kotlin/com/tatoh/dokushorenshu/ui/lector/TextoConFurigana.kt \
        app/app/src/main/kotlin/com/tatoh/dokushorenshu/datos/progreso/PrefsRepo.kt \
        app/app/src/test/kotlin/com/tatoh/dokushorenshu/ui/lector/LectorViewModelTest.kt \
        app/app/src/test/kotlin/com/tatoh/dokushorenshu/datos/progreso/ProgresoDaoTest.kt
git commit -m "$(cat <<'EOF'
feat(app): partición de katakana en sub-segmentos con hiragana alineada

Precómputo del ruby de katakana (Plan 3.7 Frente A): runs contiguos de
katakana dentro de sub-segmentos sin furigana ganan lecturaKana vía
conversión determinística carácter a carácter (katakanaAHiragana, ahora
top-level y reusable sin instanciar el Tokenizer de Kuromoji); furigana
de kanji intacta siempre gana. Pref katakanaActiva()/setKatakanaActiva()
en PrefsRepo, mismo patrón `!= "off"` que furigana, default ON.

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: UI — dibujo del ruby de katakana + toggle "カナ ON/OFF"

**Files:**
- Modify: `app/app/src/main/kotlin/com/tatoh/dokushorenshu/ui/lector/LectorViewModel.kt`
- Modify: `app/app/src/main/kotlin/com/tatoh/dokushorenshu/ui/lector/TextoConFurigana.kt`
- Modify: `app/app/src/main/kotlin/com/tatoh/dokushorenshu/ui/lector/LectorScreen.kt`
- Test: `app/app/src/test/kotlin/com/tatoh/dokushorenshu/ui/lector/LectorViewModelTest.kt`

**Interfaces:**
- Consumes: `PrefsRepo.katakanaActiva()`/`setKatakanaActiva()` (Task 3);
  `Segmento.lecturaKana` (Task 3); `EstadoLector.furiganaActiva`/`alternarFurigana()`
  como patrón de referencia (mismo flujo, replicado).
- Produces:
  - `EstadoLector.katakanaActiva: Boolean = true` — nuevo campo, mismo patrón que
    `furiganaActiva`.
  - `LectorViewModel.alternarKatakana(): Unit` — mismo patrón que `alternarFurigana()`.
  - `TextoConFurigana(tokens, gruposFurigana, furiganaActiva, katakanaActiva: Boolean, onTapPalabra)`
    — nuevo parámetro; el toggle SOLO decide qué se dibuja, nunca recomputa nada
    (los `Segmento` ya traen `lecturaKana` precomputada desde Task 3).

- [ ] **Step 1: Write the failing test**

Agregar a `LectorViewModelTest.kt`, junto al test `alternar furigana persiste`:

```kotlin
    @Test
    fun `cargar establece katakanaActiva desde prefs (default true)`() = runTest {
        val dao = ProgresoDaoFake()
        val vm = vmMomotaro(dao)
        vm.cargar(); advanceUntilIdle()
        assertTrue(vm.estado.value.katakanaActiva)
    }

    @Test
    fun `alternar katakana persiste`() = runTest {
        val dao = ProgresoDaoFake()
        val vm = vmMomotaro(dao)
        vm.cargar(); advanceUntilIdle()
        assertTrue(vm.estado.value.katakanaActiva)
        vm.alternarKatakana(); advanceUntilIdle()
        assertFalse(vm.estado.value.katakanaActiva)
        assertEquals("off", dao.pref("katakana"))
    }
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd app && ./gradlew testDebugUnitTest --tests '*LectorViewModelTest*'`
Expected: FAIL en compilación — `EstadoLector.katakanaActiva` y
`LectorViewModel.alternarKatakana()` no existen todavía.

- [ ] **Step 3: Write minimal implementation**

`ui/lector/LectorViewModel.kt` — cambios puntuales (mismo archivo, se muestran los
bloques que cambian; el resto queda igual):

```kotlin
data class EstadoLector(
    val titulo: String = "",
    val autor: String = "",
    val metadata: EntradaCatalogo? = null,
    val oraciones: List<OracionPlana> = emptyList(),
    val indiceActual: Int = -1,
    val furiganaActiva: Boolean = true,
    // Katakana-ruby (Plan 3.7): toggle independiente de furiganaActiva —
    // cualquier combinación de ambos es válida. Mismo patrón: precomputado en
    // Segmento.lecturaKana (Task 3), este flag solo decide si TextoConFurigana lo
    // dibuja.
    val katakanaActiva: Boolean = true,
    val consulta: ConsultaPalabra? = null,
    val progresoGuardado: Int = -1,
    val centradoPedido: Int = 0,
) {
    val enPortada: Boolean get() = indiceActual == -1
    val porcentajeLeido: Int get() =
        if (oraciones.isEmpty()) 0 else ((indiceActual.coerceAtLeast(0) + 1) * 100) / oraciones.size
}

/** Resultado intermedio de cargar(): agrupa lo leído en IO antes de publicar el estado. */
private data class DatosCarga(
    val historia: Historia,
    val metadata: EntradaCatalogo?,
    val planas: List<OracionPlana>,
    val furiganaActiva: Boolean,
    val katakanaActiva: Boolean,
    val progreso: ProgresoHistoria?,
)
```

```kotlin
                DatosCarga(
                    historia, metadata, planas,
                    prefs.furiganaActiva(), prefs.katakanaActiva(),
                    progresoDao.progreso(idHistoria),
                )
```

```kotlin
            _estado.value = EstadoLector(
                titulo = datos.historia.titulo,
                autor = datos.historia.autor,
                metadata = datos.metadata,
                oraciones = datos.planas,
                indiceActual = indice,
                furiganaActiva = datos.furiganaActiva,
                katakanaActiva = datos.katakanaActiva,
                progresoGuardado = indice,
                centradoPedido = 1,
            )
```

```kotlin
    fun alternarFurigana() {
        val nueva = !_estado.value.furiganaActiva
        _estado.value = _estado.value.copy(furiganaActiva = nueva)
        viewModelScope.launch(ioDispatcher) { prefs.setFuriganaActiva(nueva) }
    }

    fun alternarKatakana() {
        val nueva = !_estado.value.katakanaActiva
        _estado.value = _estado.value.copy(katakanaActiva = nueva)
        viewModelScope.launch(ioDispatcher) { prefs.setKatakanaActiva(nueva) }
    }
```

`ui/lector/TextoConFurigana.kt` — reemplazar `TextoConFurigana` y `FilaGrupo` por:

```kotlin
@Composable
fun TextoConFurigana(
    tokens: List<PalabraToken>,
    gruposFurigana: List<GrupoFurigana>,
    furiganaActiva: Boolean,
    // Katakana-ruby (Plan 3.7): decide SOLO el dibujo de Segmento.lecturaKana, ya
    // precomputada (Task 3) tanto en gruposFurigana como acá abajo — nunca dispara
    // trabajo nuevo. Independiente de furiganaActiva: cualquier combinación es válida.
    katakanaActiva: Boolean,
    onTapPalabra: ((PalabraToken) -> Unit)?,
) {
    val estiloBase = MaterialTheme.typography.headlineMedium
    FlowRow {
        if (furiganaActiva) {
            for (grupo in gruposFurigana) {
                FilaGrupo(grupo.segmentos, estiloBase, katakanaActiva, onTapPalabra)
            }
        } else {
            // Con la furigana apagada cada token es su propio "grupo" (sin cruzar
            // límites, ver doc de arriba). La partición de katakana sobre un único
            // segmento sin furigana es tan barata (scan lineal, sin diccionario) como
            // ya lo era construir el Segmento acá mismo: no hace falta precomputarla.
            for (token in tokens) {
                val segmentos = particionarPorKatakana(listOf(Segmento(token.superficie, null, token)))
                FilaGrupo(segmentos, estiloBase, katakanaActiva, onTapPalabra)
            }
        }
    }
}

@Composable
private fun FilaGrupo(
    segmentos: List<Segmento>,
    estiloBase: TextStyle,
    katakanaActiva: Boolean,
    onTapPalabra: ((PalabraToken) -> Unit)?,
) {
    Row {
        for (segmento in segmentos) {
            Column(
                horizontalAlignment = Alignment.CenterHorizontally,
                modifier = if (segmento.token.esContenido && onTapPalabra != null) {
                    Modifier.clickable { onTapPalabra(segmento.token) }
                } else Modifier,
            ) {
                // Furigana de kanji manda; si no hay, se dibuja lecturaKana SOLO
                // si el toggle de katakana está activo (Plan 3.7) — el dato ya
                // viene precomputado, esto nunca calcula nada.
                val ruby = segmento.lectura ?: (segmento.lecturaKana.takeIf { katakanaActiva })
                Text(
                    text = ruby ?: " ",
                    fontSize = 12.sp,
                    color = MaterialTheme.colorScheme.onSurfaceVariant,
                )
                Text(text = segmento.texto, style = estiloBase)
            }
        }
    }
}
```

`ui/lector/LectorScreen.kt` — dos cambios puntuales:

1. Toggle en la barra superior, junto al de Furigana:

```kotlin
                actions = {
                    if (!estado.enPortada) {
                        TextButton(onClick = vm::alternarFurigana) {
                            Text(if (estado.furiganaActiva) "Furigana ON" else "Furigana OFF")
                        }
                        TextButton(onClick = vm::alternarKatakana) {
                            Text(if (estado.katakanaActiva) "カナ ON" else "カナ OFF")
                        }
                    }
                },
```

2. Propagar `katakanaActiva` hasta `TextoConFurigana` (dos sitios: `itemsIndexed` y
`ItemOracion`):

```kotlin
                ItemOracion(
                    esActual = indice == estado.indiceActual,
                    plana = plana,
                    furiganaActiva = estado.furiganaActiva,
                    katakanaActiva = estado.katakanaActiva,
                    onTapPalabra = { token ->
                        vm.enfocar(indice)
                        vm.tocarPalabra(token)
                    },
                )
```

```kotlin
@Composable
private fun ItemOracion(
    esActual: Boolean,
    plana: OracionPlana,
    furiganaActiva: Boolean,
    katakanaActiva: Boolean,
    onTapPalabra: (PalabraToken) -> Unit,
) {
    val alphaAnimada by animateFloatAsState(
        targetValue = if (esActual) 1f else 0.35f,
        animationSpec = tween(durationMillis = 250),
        label = "alphaOracion",
    )
    Box(Modifier.alpha(alphaAnimada).fillMaxWidth()) {
        TextoConFurigana(
            tokens = plana.tokens,
            gruposFurigana = plana.gruposFurigana,
            furiganaActiva = furiganaActiva,
            katakanaActiva = katakanaActiva,
            onTapPalabra = onTapPalabra,
        )
    }
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd app && ./gradlew testDebugUnitTest --tests '*LectorViewModelTest*'` → BUILD
SUCCESSFUL. Luego gate completo:
`cd app && ./gradlew test assembleDebug` → suite completa verde (base 78 + nuevos de
Task 3 y 2) y `assembleDebug` sin errores (confirma que la UI Compose compila —
`TextoConFurigana`/`LectorScreen`/`ItemOracion` no tienen test instrumentado en este
repo, el gate es compilación + los tests de VM).

- [ ] **Step 5: Commit**

```bash
git add app/app/src/main/kotlin/com/tatoh/dokushorenshu/ui/lector/LectorViewModel.kt \
        app/app/src/main/kotlin/com/tatoh/dokushorenshu/ui/lector/TextoConFurigana.kt \
        app/app/src/main/kotlin/com/tatoh/dokushorenshu/ui/lector/LectorScreen.kt \
        app/app/src/test/kotlin/com/tatoh/dokushorenshu/ui/lector/LectorViewModelTest.kt
git commit -m "$(cat <<'EOF'
feat(app): ruby de katakana en el lector + toggle カナ ON/OFF

Frente A del Plan 3.7: TextoConFurigana dibuja Segmento.lecturaKana
(mismo estilo 12sp del ruby de furigana) cuando katakanaActiva; toggle
"カナ ON/OFF" en la barra del lector, independiente del de Furigana,
mismo patrón de persistencia/estado que furiganaActiva (precomputado en
Task 3, este toggle solo decide el dibujo).

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>
EOF
)"
```

---

### Task 5: Validación en dispositivo + ESTADO.md + PR

**Files:**
- Modify: `docs/ESTADO.md`

**Interfaces:**
- Consumes: APK con todo el plan; tablet o POCO conectado y autorizado.
- Produces: PR del branch.

- [ ] **Step 1: Validar en dispositivo** (el controller coordina en sesión principal):

```bash
cd app && ./gradlew installDebug
```
Checklist:
1. Abrir momotaro → oración con onomatopeya (ドンブラコッコ、スッコッコ。): con カナ ON se ve どんぶらこっこ arriba, alineada al run.
2. Toggle カナ OFF → desaparece el ruby de katakana; furigana de kanji intacta. Matar la app y reabrir → カナ sigue OFF (persiste). Volver a ON.
3. Combinaciones: Furigana OFF + カナ ON (solo ruby de katakana), ambos OFF (texto pelado).
4. Oración 「おばあさん、今帰ったよ。」: furigana いま sobre 今 y かえ sobre 帰, cada una en su lugar (spans regenerados, sin clamp).
5. Regresión rápida del lector: scroll libre + snap + tap palabra + sheet.

Si algo falla: fix con TDD donde sea testeable, re-validar.

- [ ] **Step 2: ESTADO.md**

Tabla: fila | 3.7 | katakana-ruby + fix alineador | ✅ Completo (PR pendiente — actualizar con #N al abrir) |. Datos operativos: catálogo regenerado con spans disjuntos (check en verify_catalogo); lector con toggle カナ (pref `katakana`, default ON). Backlog: quitar la línea del bug del alineador (resuelto); mantener el clamp de la app como defensa. Commit `docs(ESTADO): plan 3.7 completo`.

- [ ] **Step 3: Push + PR**

```bash
git push -u origin feature/plan-3.7-katakana
gh pr create --title "Plan 3.7: katakana-ruby + fix del alineador de furigana" \
  --body "Lectura en hiragana sobre runs de katakana (toggle カナ independiente, default ON, precomputado en el VM) + fix del alineador de Aozora (rubies sin ｜ ya no invaden el ruby anterior — 13 spans solapados corregidos en 3 cuentos) + check de spans disjuntos en verify_catalogo + catálogo regenerado. Spec: docs/superpowers/specs/2026-07-09-plan-3.7-katakana-design.md

🤖 Generated with [Claude Code](https://claude.com/claude-code)"
```
